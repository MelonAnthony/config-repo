#feign.client.config.product.readTimeout=6000
#feign.client.config.product.connectTimeout=1000
spring:
  application:
    name: order
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 529361105
    url: jdbc:mysql://localhost:3306/springcloud_sell?characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
env:
  test
